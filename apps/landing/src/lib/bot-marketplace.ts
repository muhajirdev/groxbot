import { BOT_MARKETPLACE_CATALOG } from "@groxbot/contracts";

/** Landing hire list — same static catalog as the office marketplace modal. */
export const LANDING_HIRE_BOTS = BOT_MARKETPLACE_CATALOG;
